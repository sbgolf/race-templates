# Production Pipeline

Customer sites are created as thin `sbgolf/[customer-slug]` repos after intake. They import `race-templates`, provide customer config/assets, deploy a staging preview, pass launch gates, then proceed to customer review and Steve sign-off before production launch.
